{
  "name": "capacitor-auto-lock-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx cap serve",
    "build": "npx cap copy"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/cli": "^5.0.0"
  }
}
