{
  "name": "fps-autolock",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "start": "npx cap serve",
    "build": "echo 'Build not needed for web only app'"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
